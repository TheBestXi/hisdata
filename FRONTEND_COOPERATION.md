# 前后端协作开发指南 - 第一阶段：挂号功能

## 1. 总体说明
后端已完成基础框架搭建，数据库表结构已自动生成。目前优先打通 **“门诊挂号”** 核心流程。

*   **API 根路径**: `http://localhost:8080/api`
*   **文档地址**: `http://localhost:8080/swagger-ui.html` (启动服务后访问)

## 2. 接口响应规范
所有接口均返回 JSON 格式，结构如下：
```json
{
  "code": 200,          // 200:成功, 500:业务异常, 401:未登录
  "message": "操作成功", // 提示信息
  "data": { ... },      // 业务数据
  "timestamp": 1710234567890
}
```

## 3. 挂号业务流程 (Frontend Workflow)

前端开发人员请按照以下流程开发挂号页面：

### 步骤 1：患者识别
*   **场景**: 挂号前需确认是谁在挂号。
*   **动作**: 输入患者姓名进行搜索。
*   **API**: `GET /api/patient/search?name=张三`
*   **展示**: 下拉列表显示匹配的患者（包含身份证号脱敏显示），用户选中一个。

### 步骤 2：选择医生
*   **场景**: 患者选定后，需选择挂哪个医生的号。
*   **动作**:
    1.  (可选) 先选择科室（目前可做成静态下拉框：内科、外科、儿科）。
    2.  根据科室查询医生列表。
*   **API**: `GET /api/doctor?department=内科`
*   **展示**: 医生卡片列表（显示姓名、职称、所属科室）。

### 步骤 3：确认挂号
*   **场景**: 选中医生后，展示挂号详情（日期、费用），用户确认支付/提交。
*   **动作**: 点击“确认挂号”按钮。
*   **API**: `POST /api/appointment`
*   **参数**:
    ```json
    {
      "patientId": 1,
      "doctorId": 2,
      "department": "内科",
      "registrationDate": "2025-12-13",
      "registrationFee": 50.00
    }
    ```

### 步骤 4：查看结果
*   **场景**: 挂号成功后跳转到详情页或列表页。
*   **API**: `GET /api/appointment/patient/{patientId}` (查询某患者的挂号记录)

## 4. 接口开发进度

| 接口功能 | URL | 方法 | 后端状态 | 负责人 |
| :--- | :--- | :--- | :--- | :--- |
| **搜索患者** | `/api/patient/search` | GET | ✅ 已就绪 | Backend |
| **查询医生** | `/api/doctor` | GET | ✅ 已就绪 | Backend |
| **创建挂号** | `/api/appointment` | POST | ✅ 已就绪 | Backend |
| **挂号记录** | `/api/appointment/patient/{id}` | GET | ⏳ 开发中 | Backend |

---

## 5. 待办事项 (To-Do)
*   [前端] 搭建 Vue3 项目，封装 Axios 拦截器。
*   [前端] 实现上述 3 个步骤的页面。
*   [后端] 提供“挂号记录查询”接口。
