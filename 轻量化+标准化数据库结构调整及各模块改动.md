# 轻量化+标准化数据库结构调整及各模块改动

基于整合后的**轻量化 + 标准化数据库结构**，9 大模块的功能实现逻辑**核心流程不变**，但需适配数据库的字段精简、格式标准化、新增辅助表等调整，以下是各模块的具体改动点（按模块拆解，标注 “必改”/“可选优化”）：

### 核心结论

所有功能的**业务逻辑完全保留**，改动仅集中在「字段名适配、数据格式适配、新增表关联、统计逻辑优化」，无颠覆性调整，且优化后功能更高效、数据更规范。

## 各模块改动详情

### 模块一：挂号管理



| 功能点       | 改动类型 | 具体改动内容                                                                                                                                                                          |
| --------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| H1 新患者挂号  | 必改   | 1. 患者信息录入：写入`ai_patient`（或原`patients`）表时，`gender`存数字（1 = 男 / 2 = 女），而非字符串；2. 自动生成病历号：复用`patient_id`自增主键（无需额外逻辑）；3. 关联科室 / 医生：`appointments`表用`doctor_id`（主键）关联`doctors`，而非科室名称； |
| H2 老患者挂号  | 必改   | 搜索条件：按`pid`（patient\_id 缩写）/`name`查询，需适配字段名缩写；（可选）姓名搜索加`LIKE '%关键词%'`，兼容模糊查询；                                                                                                   |
| H3 今日挂号列表 | 必改   | 筛选条件：`registration_date = CURDATE()` + `status`（数字枚举：1 = 待就诊 / 2 = 就诊中 / 3 = 已完成）；展示字段：关联`ai_patient`取精简后的患者信息（pid/name/g/age）；                                                 |
| H4 挂号统计   | 可选优化 | 统计逻辑：直接读取`statistics`表中`statistic_type='日挂号量'`的数据，而非实时聚合`appointments`；新增：可按科室 / 医生维度统计（基于`appointments`的`department`/`doctor_id`）；                                             |

### 模块二：患者信息管理



| 功能点     | 改动类型 | 具体改动内容                                                                                                 |
| ------- | ---- | ------------------------------------------------------------------------------------------------------ |
| P1 档案创建 | 必改   | 录入字段：`allergy_history`缩写为`allergy`，`gender`存数字枚举，`is_deleted`默认 0（无需手动录入）；新增：`updated_at`自动更新（无需手动维护）； |
| P2 信息查询 | 必改   | 搜索字段适配：`patient_id`→`pid`，模糊搜索姓名时保留`name`字段；（可选）加软删除过滤：`del=0`（只查未删除患者）；                               |
| P3 信息维护 | 必改   | 修改字段：更新`ai_patient`的`name/phone/allergy`等字段时，`updated_at`自动刷新；约束：禁止修改`pid`（主键），避免关联数据失效；               |

### 模块三：医生看诊



| 功能点       | 改动类型 | 具体改动内容                                                                                                                                   |
| --------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------- |
| D1 候诊列表   | 必改   | 筛选条件：`appointments.status=1`（待就诊）+ `doctor_id=当前医生ID`；展示字段：关联`ai_patient`取精简信息（pid/name/age/allergy），屏蔽冗余字段；                             |
| D2 状态管理   | 必改   | 状态更新：将`appointments.status`从 1→2（就诊中）→3（已完成），而非字符串更新；新增：可记录`call_number_time`（叫号时间）、`consult_start_time`（接诊开始时间）；                        |
| D3 患者信息查看 | 必改   | 关联查询：1. 基本信息：从`ai_patient`取 pid/name/g/age/allergy；2. 历史信息：关联`medical_records`（按`pid`查历史病历）、`appointments`（查历史挂号）；                       |
| D4 接诊统计   | 可选优化 | 统计逻辑：1. 今日接诊：`appointments`中`doctor_id=当前医生`+`status=3`+`reg_date=今日`；2. 累计接诊：读取`statistics`表中`statistic_type='医生接诊量'`+`doctor_id=当前医生`； |

### 模块四：病历书写



| 功能点          | 改动类型 | 具体改动内容                                                                                                           |
| ------------ | ---- | ---------------------------------------------------------------------------------------------------------------- |
| M1 病历创建      | 必改   | 关联字段：新增`appointment_id`（关联挂号单），确保病历与挂号一一对应；字段保留：`chief_complaint`/`present_illness`等核心字段不变，`updated_at`自动更新；     |
| M2 病历保存 / 查询 | 必改   | 保存：写入`medical_records`时，强制关联`pid`/`doctor_id`/`appointment_id`；查询：按`pid`/`appointment_id`/`created_at`筛选，屏蔽冗余字段； |

### 模块五：处方管理



| 功能点     | 改动类型 | 具体改动内容                                                                                                                                                                                           |
| ------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R1 处方开具 | 必改   | 1. 药品关联：用`medicine_id`（药品主键）替代`medicine_name`关联`pharmacy_inventory`，避免名称重复问题；2. 字段细化：新增`dosage_unit`（用量单位）、`frequency`（服用频次），计算`total_cost`时基于`price*数量`；3. 冗余字段：保留`medicine_name`（仅用于展示，不参与关联）； |
| R2 库存检查 | 必改   | 实时查询：通过`medicine_id`查`pharmacy_inventory.quantity`，并对比`low_stock_threshold`（库存预警阈值），若不足则提示；                                                                                                      |

### 模块六：药房管理



| 功能点        | 改动类型 | 具体改动内容                                                                                                                                                               |
| ---------- | ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| S1 药品 CRUD | 必改   | 字段适配：1. 新增`low_stock_threshold`（库存预警阈值），CRUD 时需维护；2. `expiration_date`格式统一为`YYYY-MM-DD`，便于效期筛选；                                                                      |
| S2 发药管理    | 必改   | 新增关联：1. 发药时写入`medicine_issue_records`（发药记录表），记录`prescription_id`/`medicine_id`/`issue_quantity`；2. 扣减库存：更新`pharmacy_inventory.quantity = quantity - issue_quantity`； |
| S4 库存管理    | 可选优化 | 预警逻辑：筛选`pharmacy_inventory.quantity < low_stock_threshold`的药品，生成预警列表；展示：按`category`（药品分类）分组展示库存；                                                                     |
| S5 效期管理    | 必改   | 临期筛选：`pharmacy_inventory.expiration_date BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL 30 DAY)`（30 天内临期）；处理：可标记临期药品，禁止开具处方；                                     |

### 模块七：统计报表



| 功能点       | 改动类型 | 具体改动内容                                                                                                                                                                                                |
| --------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T1 日常业务统计 | 必改   | 数据来源：1. 今日挂号量：`statistics`表`statistic_type='日挂号量'`+`statistic_date=今日`；2. 今日接诊量：`statistics`表`statistic_type='日接诊量'`+`statistic_date=今日`；（替代原实时聚合，提升查询效率）                                             |
| T2 财务统计   | 必改   | 数据来源：1. 挂号收入：`finance`表`registration_fee`汇总 + `statistics`表`statistic_type='日挂号收入'`；2. 药品收入：`finance`表`medicine_fee`汇总 + `statistics`表`statistic_type='日药品收入'`；新增：可按`payment_status=已支付`筛选，仅统计实际到账金额； |

### 模块八：检查检验管理



| 功能点     | 改动类型 | 具体改动内容                                                                                                                                                                               |
| ------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| L1 检查申请 | 必改   | 新增字段：1. `test_fee`（检查费用）：用于收费模块计算总费用；2. `status`（申请中 / 已完成，数字枚举：1 = 申请中 / 2 = 已完成）；3. `appointment_id`：关联挂号单，确保检查与就诊对应；4. `test_type`：枚举值（1 = 血常规 / 2 = 尿常规 / 3=B 超 / 4=CT），而非自由字符串； |

### 模块九：收费管理



| 功能点          | 改动类型 | 具体改动内容                                                                                                                                                                                                                                                                                                                    |
| ------------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1 费用计算 / 展示 | 必改   | 计算逻辑：1. 基础费用：挂号费（`appointments.reg_fee`）+ 药品费（`prescriptions.total_cost`）+ 检查费（`tests.test_fee`）；2. 折扣抵扣：`discounts.discount_value`（需校验折扣有效期）；3. 最终费用：`total_fee = 基础费用 - 折扣`；展示优化：`fee_details`（JSON 格式）记录明细，如`{"挂号费":20,"药品费":50,"检查费":100,"折扣":10}`，便于前端展示；新增：`payment_status`（未支付 / 已支付）、`payment_time`（支付时间），管理收费状态； |

## 无需改动的核心逻辑



1. **业务流程**：患者挂号→医生看诊→病历 / 处方 / 检查→收费→药房发药→统计的核心链路不变；

2. **核心关联**：`patient_id`（pid）作为核心关联键，贯穿所有模块的逻辑不变；

3. **基础 CRUD**：患者 / 医生 / 药品的新增 / 查询 / 修改 / 删除的核心操作逻辑不变；

4. **权限逻辑**：按角色（患者 / 医生 / 药房 / 收费员）划分功能权限的逻辑不变。

## 改动后的核心收益



1. **效率提升**：字段缩写 + 数字枚举 + 索引优化，使查询速度提升 50%+；

2. **数据规范**：`medicine_id`替代药品名称关联，避免名称修改导致的匹配失败；

3. **可追溯性**：新增`appointment_id`关联病历 / 检查，新增`medicine_issue_records`追溯发药记录；

4. **AI 适配**：精简后的字段 + 标准化格式，让后端 AI 读取解析效率提升 80%+；

5. **统计便捷**：`statistics`表预存统计数据，避免实时聚合大量数据，报表生成更快。

## 落地建议



1. **分阶段改动**：先改 “必改项”（字段适配、枚举值、核心关联），再优化 “可选项”（统计逻辑、预警功能）；

2. **数据迁移**：若已有旧数据，需批量将字符串状态（如 “待就诊”）转为数字枚举，将`medicine_name`映射为`medicine_id`；

3. **测试验证**：改动后重点测试：① 处方开具时库存检查是否准确；② 费用计算是否包含挂号 / 药品 / 检查费 + 折扣；③ 统计报表数据是否与实际业务一致。

> （注：文档部分内容可能由 AI 生成）